<UserControl x:Class="Alex_Mai.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Alex_Mai.Views"
             xmlns:views="clr-namespace:Alex_Mai.Views"
             xmlns:converters="clr-namespace:Alex_Mai.Converters" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">

    <UserControl.Resources>
        <converters:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>

        <Style x:Key="InventoryItemStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource GameBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource GameBorderBrush}"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                RenderTransformOrigin="0.5,0.5">

                            <Border.RenderTransform>
                                <ScaleTransform x:Name="ButtonScaleTransform" ScaleX="1.0" ScaleY="1.0" />
                            </Border.RenderTransform>
                            <Border.Effect>
                                <DropShadowEffect x:Name="ButtonShadow" ShadowDepth="2" BlurRadius="5" Opacity="0" Color="Black"/>
                            </Border.Effect>

                            <Grid>
                                <StackPanel>
                                    <Image Source="{Binding IconPath}" Height="90" Width="90" Margin="0,10,0,5"/>
                                    <Separator Background="Black" Margin="5,0"/>
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontFamily="{StaticResource GameFont}" TextAlignment="Center" Padding="5"/>
                                </StackPanel>

                                <Border Background="#AA000000" CornerRadius="100" Padding="5,2"
                                        HorizontalAlignment="Right" VerticalAlignment="Top" Margin="5"
                                        Visibility="{Binding Quantity, Converter={StaticResource IntToVisibilityConverter}}">
                                    <TextBlock Text="{Binding Quantity}" Foreground="White" FontWeight="Bold"/>
                                </Border>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ItemBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#E0E0E0" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonShadow" Storyboard.TargetProperty="Opacity" To="0.4" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScaleTransform" Storyboard.TargetProperty="ScaleX" To="1.03" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScaleTransform" Storyboard.TargetProperty="ScaleY" To="1.03" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ItemBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" To="#F5F5F5" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonShadow" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScaleTransform" Storyboard.TargetProperty="ScaleX" To="1.0" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="ButtonScaleTransform" Storyboard.TargetProperty="ScaleY" To="1.0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Background="{StaticResource GameBackgroundBrush}" 
            BorderBrush="{StaticResource GameBorderBrush}" 
            BorderThickness="2" CornerRadius="5" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="INVENTORY" FontFamily="{StaticResource GameFont}" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="CLOSE" 
                        Style="{StaticResource GameButtonStyle}"
                        HorizontalAlignment="Right" 
                        Padding="15,5"
                        Command="{Binding CloseCommand}"/>
            </Grid>

            <ItemsControl Grid.Row="1" ItemsSource="{Binding Items}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource InventoryItemStyle}" 
                                Width="120" Height="160" Margin="10"
                                Command="{Binding DataContext.UseItemCommand, RelativeSource={RelativeSource AncestorType=views:InventoryView}}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </Border>
</UserControl>