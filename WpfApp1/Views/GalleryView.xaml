<UserControl x:Class="Alex_Mai.Views.GalleryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Alex_Mai.Views"
             xmlns:viewModels="clr-namespace:Alex_Mai.ViewModels"
             xmlns:converters="clr-namespace:Alex_Mai.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:GalleryViewModel}"
             d:DesignHeight="500" d:DesignWidth="280" Background="Black">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter"/>

        <Style x:Key="GalleryThumbnailStyle" TargetType="Button">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="Width" Value="80"/>
            <Setter Property="Height" Value="80"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#111111" Padding="10">
                <Grid VerticalAlignment="Center">
                    <Button Command="{Binding NavigateToHomeCommand}" VerticalAlignment="Center" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Foreground="White" Content="&lt; Back"/>
                    <TextBlock Text="Gallery" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid>
                    <ItemsControl ItemsSource="{Binding UnlockedItems}"
                                  Visibility="{Binding UnlockedItems.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=NotZero}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource GalleryThumbnailStyle}"
                                        Command="{Binding DataContext.ShowFullImageCommand, RelativeSource={RelativeSource AncestorType=local:GalleryView}}"
                                        CommandParameter="{Binding}">
                                    <Image Source="{Binding ThumbnailPath}" Stretch="UniformToFill"/>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock Text="No images unlocked yet." Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20"
                               Visibility="{Binding UnlockedItems.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=0}"/>
                </Grid>
            </ScrollViewer>
        </Grid>

        <Border Background="#CC000000"
                Visibility="{Binding IsFullImageViewVisible, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <Image Source="{Binding SelectedFullImagePath}" Stretch="Uniform" Margin="20"/>
                <Button Command="{Binding CloseFullImageCommand}"
                        Content="X" FontWeight="Bold" FontSize="16" Foreground="Black"
                        Background="White" BorderBrush="Black" BorderThickness="1"
                        Width="30" Height="30" Padding="0"
                        HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Cursor="Hand"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>