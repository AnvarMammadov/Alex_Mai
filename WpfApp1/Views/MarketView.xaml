<UserControl x:Class="Alex_Mai.Views.MarketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Alex_Mai.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">

    <Border Width="820" Height="500" Background="{StaticResource GameBackgroundBrush}" 
            BorderBrush="{StaticResource GameBorderBrush}" BorderThickness="2" CornerRadius="5" Padding="15">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="MARKET" FontFamily="{StaticResource GameFont}" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="CLOSE" Style="{StaticResource GameButtonStyle}" HorizontalAlignment="Right" Padding="15,5" Command="{Binding CloseCommand}"/>
            </Grid>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="All" Style="{StaticResource GameButtonStyle}" Margin="0,0,10,0" Command="{Binding FilterCategoryCommand}" CommandParameter="All"/>
                <Button Content="Food" Style="{StaticResource GameButtonStyle}" Margin="0,0,10,0" Command="{Binding FilterCategoryCommand}" CommandParameter="Food"/>
                <Button Content="Other" Style="{StaticResource GameButtonStyle}" Margin="0,0,10,0" Command="{Binding FilterCategoryCommand}" CommandParameter="Other"/>
            </StackPanel>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding DisplayItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="5" Padding="10" Width="175" Height="240">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="60"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Image Grid.Row="0" Source="{Binding IconPath}" Height="80" Width="80" Margin="5" HorizontalAlignment="Center"/>

                                    <TextBlock Grid.Row="1" Text="{Binding Name}" FontWeight="Bold" HorizontalAlignment="Center" 
                                               FontFamily="{StaticResource GameFont}" FontSize="16" Margin="0,5,0,0"/>

                                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,2,0,5">
                                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" HorizontalAlignment="Center" 
                                                   FontFamily="{StaticResource GameFont}" FontSize="12" VerticalAlignment="Top"/>
                                    </ScrollViewer>

                                    <Button Grid.Row="3" Style="{StaticResource GameButtonStyle}"
                                            VerticalAlignment="Bottom" HorizontalAlignment="Center" Width="120"
                                            Command="{Binding DataContext.BuyItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}">
                                        <TextBlock>
                                            <Run Text="Buy"/>
                                            <Run Text="{Binding Price, StringFormat={}({0} $)}"/>
                                        </TextBlock>
                                    </Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>