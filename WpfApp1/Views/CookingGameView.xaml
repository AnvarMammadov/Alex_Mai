<UserControl x:Class="Alex_Mai.Views.CookingGameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Alex_Mai.Views"
             xmlns:converters="clr-namespace:Alex_Mai.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Border Width="700" Height="450" Background="{StaticResource GameBackgroundBrush}" 
            BorderBrush="{StaticResource GameBorderBrush}" BorderThickness="2" CornerRadius="5">
        <Grid>
            <Grid Visibility="{Binding IsCookingScreenVisible, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                <TextBlock Text="Nə Bişirmək İstəyirsən?" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="20"
                           FontFamily="{StaticResource GameFont}" FontSize="28" FontWeight="Bold"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="20,80,20,20">
                    <ItemsControl ItemsSource="{Binding AvailableRecipes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Content="{Binding Name}" Style="{StaticResource GameButtonStyle}" Margin="5" Width="250"
                                        Command="{Binding DataContext.SelectRecipeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>

            <Grid Visibility="{Binding IsCookingScreenVisible, Converter={StaticResource BoolToVisibilityConverter}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Padding="10">
                    <ItemsControl ItemsSource="{Binding CurrentIngredients}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource GameButtonStyle}" Margin="5" Width="80" Height="80"
                                        Command="{Binding DataContext.AddIngredientCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">
                                    <Grid>
                                        <Image Source="{Binding IconPath}" Width="48" Height="48"/>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Bottom" HorizontalAlignment="Center" 
                                                   FontSize="12" Foreground="Black" Background="#AAFFFFFF"
                                                   FontFamily="{StaticResource GameFont}"/>
                                    </Grid>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Border Grid.Column="1" BorderBrush="#DDDDDD" BorderThickness="2,0,0,0">
                    <Image Source="{Binding DishImagePath}" Stretch="Uniform" Margin="10"/>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>